RewriteEngine On

# Si es un archivo o directorio que existe, servirlo directamente
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Todo lo demás redirigir a index.html (SPA)
RewriteRule ^ index.html [QSA,L]
